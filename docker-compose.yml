services:
  postgres:
    image: postgres:15-alpine
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: password
      POSTGRES_DB: postgres
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 5s
      timeout: 5s
      retries: 5

  zookeeper:
    image: confluentinc/cp-zookeeper:latest
    environment:
      ZOOKEEPER_CLIENT_PORT: 2181
      ZOOKEEPER_TICK_TIME: 2000
    ports:
      - "2181:2181"

  kafka:
    image: confluentinc/cp-kafka:7.4.0
    depends_on:
      - zookeeper
    environment:
      KAFKA_BROKER_ID: 1
      KAFKA_ZOOKEEPER_CONNECT: zookeeper:2181
      KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
      KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT
      KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092
      KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://host.docker.internal:9496
      KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    # image: confluentinc/cp-kafka:latest
    # environment:
    #   KAFKA_PROCESS_ROLES: broker,controller
    #   KAFKA_NODE_ID: 1
    #   KAFKA_CONTROLLER_QUORUM_VOTERS: 1@localhost:9093
    #   KAFKA_LISTENERS: PLAINTEXT://0.0.0.0:9092,CONTROLLER://0.0.0.0:9093
    #   KAFKA_ADVERTISED_LISTENERS: PLAINTEXT://localhost:9092
    #   KAFKA_LISTENER_SECURITY_PROTOCOL_MAP: PLAINTEXT:PLAINTEXT,CONTROLLER:PLAINTEXT
    #   KAFKA_INTER_BROKER_LISTENER_NAME: PLAINTEXT
    #   KAFKA_OFFSETS_TOPIC_REPLICATION_FACTOR: 1
    #   KAFKA_LOG_DIRS: /var/lib/kafka/data
    ports:
      - "9496:9092"
    healthcheck:
      test: ["CMD-SHELL", "kafka-topics --bootstrap-server 127.0.0.1:9092 --list"]
      interval: 10s
      timeout: 10s
      retries: 10
      start_period: 30s

  # notification-service:
  #   build:
  #     context: ../notification-service
  #     dockerfile: Dockerfile
  #   container_name: notification-app
  #   ports:
  #     - "8083:8083"
  #   depends_on:
  #     kafka:
  #       condition: service_healthy
  #     mailpit:
  #       condition: service_started
  #   environment:
  #     SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  #     SPRING_MAIL_HOST: mailpit
  #     SPRING_MAIL_PORT: 1025
  #     # можно переопределить логирование
  #     LOG_LEVEL: DEBUG
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://127.0.0.1:8083/actuator/health"]
  #     interval: 30s
  #     timeout: 3s
  #     retries: 3

  mailpit:
    image: axllent/mailpit
    container_name: notification-mailpit
    ports:
      - "10025:1025"   # SMTP
      - "8025:8025"   # UI

  # app:
  #   build: .
  #   image: user-service:latest
  #   container_name: user-app
  #   ports:
  #     - "8443:8443"
  #     - "8081:8081"
  #   depends_on:
  #     postgres:
  #       condition: service_healthy
  #     kafka:
  #       condition: service_healthy
  #   environment:
  #     SPRING_DATASOURCE_URL: jdbc:postgresql://postgres:5432/postgres
  #     SPRING_DATASOURCE_USERNAME: postgres
  #     SPRING_DATASOURCE_PASSWORD: password
  #     SPRING_KAFKA_BOOTSTRAP_SERVERS: kafka:9092
  #     APP_NOTIFICATION_SERVICE_URL: http://notification-service:8083/api/notifications
  #     # Для HTTPS внутри контейнера используем тот же keystore
  #     KEYSTORE_PASSWORD: changeit
  #   healthcheck:
  #     test: ["CMD", "curl", "-f", "http://localhost:8081/actuator/health"]
  #     interval: 30s
  #     timeout: 3s
  #     retries: 3

volumes:
  postgres_data:
  # kafka_data:
